[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "evolife"
version = "0.1.0"
description = "AI-Driven Wildlife & Ecosystem Simulator"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.setuptools.packages.find]
where = ["."]
include = ["evolife*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
